package nothing;

import java.math.*;
import java.util.*;
public class Main {
  public static void main(String[] args){
    Scanner  input = new  Scanner(System.in);
    int n = input.nextInt();
    for(int i = 1;i <= n;i ++)
    {
    	BigInteger b1 = new BigInteger(input.next());//,b2 = new BigInteger(input.next());    
    	System.out.println(b1);
       /* if(i != 1)
         System.out.println("");
         System.out.println("Case " + i + ":");
         System.out.println(b1 + " + " + b2 + " = " + b2.add(b1));
         */
    }
   }
}
